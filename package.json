{
  "name": "mini-trello",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "echo 'No tests configured yet.'",
    "check": "pnpm -r check"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "turbo": "^2.5.4"
  }
}
