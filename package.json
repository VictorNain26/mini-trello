{
  "name": "mini-trello",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "echo 'No tests configured yet.'",
    "check": "pnpm -r check"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
