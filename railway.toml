[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "echo 'Root service - no start command'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 1