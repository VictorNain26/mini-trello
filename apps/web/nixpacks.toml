[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "./setup-pnpm.sh",
  "cd ../..",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "cd ../..",
  "pnpm --filter @mini-trello/web build"
]

[start]
cmd = "cd apps/web && pnpm preview"