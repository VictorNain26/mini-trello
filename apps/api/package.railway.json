{
  "name": "mini-trello-api",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter api build && cd apps/api && pnpm prisma generate",
    "start": "npx prisma migrate deploy && node dist/index.js",
    "railway:build": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter api build && cd apps/api && pnpm prisma generate",
    "railway:start": "npx prisma migrate deploy && node dist/index.js"
  },
  "dependencies": {
    "@auth/express": "^0.6.1",
    "@prisma/client": "^5.23.0",
    "@trpc/server": "^10.45.2",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "morgan": "^1.10.0",
    "pug": "^3.0.3",
    "socket.io": "^4.8.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.3",
    "@types/morgan": "^1.9.9",
    "@types/node": "^24.0.7",
    "prisma": "^5.23.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}